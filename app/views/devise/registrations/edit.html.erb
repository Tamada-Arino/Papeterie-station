<h2>
  <%= link_to :back, class: "text-decoration-none" do %>
    <i class="bi bi-arrow-left-circle text-secondary"></i>
  <% end %>
  登録情報の編集
</h2>
<%= render "devise/shared/error_messages", resource: resource %>
<div class="mx-auto p-3 mt-4 shadow rounded">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <div class="field p-2">
      <%= f.label :email %><br />
      <%= f.email_field :email, class: "w-100", autofocus: true, autocomplete: "email" %>
    </div>
    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div><%= t('.currently_waiting_confirmation_for_email', email: resource.unconfirmed_email) %></div>
    <% end %>
    <div class="field p-2">
      <%= f.label :password %> <i>(<%= t('.leave_blank_if_you_don_t_want_to_change_it') %>)</i><br />
      <%= f.password_field :password, class: "w-100", autocomplete: "new-password" %>
      <% if @minimum_password_length %>
        <br />
        <em><%= t('devise.shared.minimum_password_length', count: @minimum_password_length) %></em>
      <% end %>
    </div>
    <div class="field p-2">
      <%= f.label :password_confirmation %><br />
      <%= f.password_field :password_confirmation, class: "w-100", autocomplete: "new-password" %>
    </div>
    <div class="field p-2">
      <%= f.label :current_password %><br />
      <%= f.password_field :current_password, class: "w-100", autocomplete: "current-password" %><br />
      <i>(<%= t('.we_need_your_current_password_to_confirm_your_changes') %>)</i>
    </div>
    <div class="actions mt-4">
      <%= f.submit t('.update'), class:"btn btn-success text-light w-100 py-2" %>
    </div>
  <% end %>
</div>
<div class="mx-auto p-3 mt-5 bg-dark text-white shadow rounded">
  お気に召しませんでしたか？
  <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#confirmationModal">
    <%= t('.cancel_my_account') %>
  </button>
</div>
<div class="modal fade" id="confirmationModal" tabindex="-1" aria-labelledby="confirmationModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="confirmationModalLabel">削除確認</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        本当に削除しますか？（削除後同じメールアドレスは登録できません）
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">キャンセル</button>
        <%= button_to "削除する", registration_path(resource_name), class:"btn btn-danger", method: :delete %>
      </div>
    </div>
  </div>
</div>
